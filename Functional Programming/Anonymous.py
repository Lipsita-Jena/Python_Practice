# Function without name is Anonymous Function
# When we are using the function only once.
# They are also called as lambda expressions.

result=lambda a:a*a
print(result(5))

# here a : is the argument, lambda is used in case of anonymous function, result should be stored in a var 